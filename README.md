# 🏗️ Rest Project: Django Backend & CI/CD

Комплексное API-приложение на Django с настроенной инфраструктурой, контейнеризацией и автоматическим деплоем через GitHub Actions.

## 🌟 Основные возможности
* **Django REST Framework**: Мощное API с JWT-авторизацией.
* **Celery & Redis**: Фоновые задачи и кэширование.
* **PostgreSQL**: Надежное хранение данных.
* **Nginx**: Обратный прокси для статики и медиа.
* **CI/CD**: Полная автоматизация тестирования и деплоя.

---

## 🏗 Инфраструктура и сервер
Сервер настроен для работы в Docker-окружении:
* **Безопасность**: Настроены SSH-ключи, доступ по паролю ограничен.
* **Firewall**: Разрешены только необходимые порты (22, 80, 443).
* **Управление процессами**: Systemd не требуется, так как Docker настроен на автоматический перезапуск контейнеров (`restart: always`).

---

## 📦 GitHub Actions & CI/CD
Workflow расположен в `.github/workflows/`. Деплой запускается автоматически при каждом `push` в ветку `main`.

**Этапы пайплайна:**
1.  **Тестирование**: Запуск unit-тестов. Если тесты провалены, деплой блокируется.
2.  **Очистка**: Удаление старых образов и кэша билда для предотвращения ошибки `no space left on device`.
3.  **Деплой**: Сборка свежих образов и запуск контейнеров на удаленном сервере.

---

## 🔐 Настройка секретов
Для работы CI/CD необходимо добавить следующие переменные в **GitHub Secrets**:

| Ключ | Описание |
| :--- | :--- |
| `HOST` | IP-адрес сервера |
| `USER` | Имя пользователя (например, `root`) |
| `SSH_KEY` | Приватный SSH-ключ для доступа к серверу |
| `DB_NAME` | Имя БД |
| `DB_USER` | Пользователь БД |
| `DB_PASSWORD` | Пароль к БД |
| `SECRET_KEY` | Секретный ключ Django |

*На сервере должен быть подготовлен `.env` файл на основе `.env.example`.*

---

## 🛠 Обслуживание проекта

### Проверка статуса контейнеров
```bash
docker compose ps
